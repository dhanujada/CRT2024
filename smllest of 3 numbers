smallest of 3 numbers
#include <stdio.h>

int main() {
    // Write C code here
 int a,b,c;
 scanf("%d%d%d",&a,&b,&c);
 if(a<b && a<c){
     printf(" a is small");
 }
 else if(b<c){
     printf("b is small");
 }
 else
 {
     printf("c is small");
 }


    return 0;
}
