read n elements and print search of elements
#include <stdio.h>
int main() {
int n;
scanf("%d",&n);
int a[n];
int search;
scanf("%d",&search);
for(int i=0;i<n;i++){
    scanf("%d",&a[i]);
}
for(int i=0;i<=n;i++){
    if(search==a[i]){
        printf("found");
        break;
    }
    if(i==n){
        printf("not found");
    }
}
return 0;
}
